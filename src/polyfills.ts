// src/polyfills.ts

import 'zone.js';

// On peut laisser ça si tu veux, mais ce n'est pas obligatoire
// (window as any).global = window as any;
// (window as any).process = (window as any).process || { env: {} };

export {}; // juste pour que le fichier soit traité comme module
